spring.application.name=Ticket-Booking

# TiDB Cloud Serverless MySQL Connection Settings (With Fallback Defaults)
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/ticket_booking_db?sslMode=VERIFY_IDENTITY&enabledTLSProtocols=TLSv1.2,TLSv1.3&useSSL=true&createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:4ReUz4rExeG1jWC.root}
spring.datasource.password=${DB_PASSWORD:SaUayAG3otWPOSTv}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
